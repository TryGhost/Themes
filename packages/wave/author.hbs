{{!< default}}

<div class="content-area">
    <main class="site-main container medium">
        {{#author}}
            <div class="term-author">
                {{> "author-box"}}
            </div>
        {{/author}}
        <div class="post-feed">
            {{#foreach posts}}
                {{> "loop"}}
            {{/foreach}}
        </div>
        {{pagination}}
    </main>
</div>