{{#if next}}
  <button class="pagination" data-url="{{page_url next}}">
    <span class="load-more">{{t 'Load More'}}</span>
    <span class="page-number">{{t 'Page {page} of {pages}' page=page pages=pages}}</span>
  </button>
{{/if}}