{{!< default}}

<main class="gh-main">

    {{#if @site.cover_image}}
        {{> "components/header" layout="wide" background="image" action="email"}}
    {{else}}
        {{> "components/header" layout="wide" background="" action="email"}}
    {{/if}}

    {{> "components/list" layout="magazine" count="5" showExcerpt=true showPrimaryTag=true}}

    {{#if @custom.enter_tag_slugs_for_lists}}
        {{#get "tags" filter="slug:[{{@custom.enter_tag_slugs_for_lists}}]"}}
            {{#foreach tags}}
                {{#if @odd}}
                    {{> "components/list" layout="grid" column="3" count="3" showHeader=true}}
                {{else}}
                    {{> "components/list" layout="grid" column="4" count="4" showHeader=true}}
                {{/if}}
            {{/foreach}}
        {{/get}}
    {{else}}
        {{#get "tags" include="count.posts" order="count.posts desc" limit="4"}}
            {{#foreach tags}}
                {{#if @odd}}
                    {{> "components/list" layout="grid" column="3" count="3" showHeader=true}}
                {{else}}
                    {{> "components/list" layout="grid" column="4" count="4" showHeader=true}}
                {{/if}}
            {{/foreach}}
        {{/get}}
    {{/if}}

    {{> "components/cta"}}

</main>