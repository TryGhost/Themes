<div class="widget widget-tags u-shadow">
    <h4 class="widget-title">Tags</h4>
    {{#get "tags" limit="all" include="count.posts"}}
        <div class="tag-feed">
            {{#foreach tags}}
                <div class="tag">
                    <a class="tag-link" href="{{url}}">
                        <span class="tag-name">{{name}}</span>
                        <span class="tag-count">{{count.posts}}</span>
                    </a>
                </div>
            {{/foreach}}
        </div>
    {{/get}}
</div>